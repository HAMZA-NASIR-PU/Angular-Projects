# Why it is not good to use template reference variables inside a for loop for radio buttons ? - Task 10

Because it craetes not only one but multiple radio buttons with same template-reference variable.